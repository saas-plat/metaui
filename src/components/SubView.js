import CacheRoute from 'react-router-cache-route';

export default (props) => {
  // return {
  //   page.routes.map(subview => <CacheRoute key={subview.id} path={`/${subview.id}/:id?`}
  //              render={()=>this.renderView(subview)}></CacheRoute>)
  // } {
  //   page.modals.map(subview => this.renderView(subview))
  // }
}
